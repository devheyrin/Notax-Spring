<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.notax.main.MainDAO">
    <select id="selectRank" resultType="MainRankingVO">
        SELECT DISTINCT (pd_name) AS pd_name ,p.PD_NO  , p.PD_BY_DFS_SALE, pt.PD_PRICE ,
                        pt.PD_PRICE - (pt.PD_PRICE * p.PD_BY_DFS_SALE/100) AS PD_SALE_PRICE
        FROM PRODUCTS_BY_DUTY_FREE_SHOP_TB p ,PRODUCTS_TB pt
        WHERE p.PD_BY_DFS_SALE > 0
          AND p.PD_NO = pt.PD_NO
        ORDER BY 3 desc
    </select>
</mapper>